add_subdirectory(include)
add_subdirectory(Day_01)
add_subdirectory(Day_02)
add_subdirectory(Day_03)

# target_link_libraries(safe_malloc_lib PUBLIC safe_malloc)

# target_include_directories(safe_malloc_lib PUBLIC
#                           "${PROJECT_BINARY_DIR}"
#                           )